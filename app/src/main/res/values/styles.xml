<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="GalleryToolbarTitle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="GalleryNavigationButton">
        <item name="android:layout_width">@dimen/gallery_toolbar_icon_size</item>
        <item name="android:layout_height">@dimen/gallery_toolbar_icon_size</item>
        <item name="android:layout_marginLeft">@dimen/gallery_toolbar_icon_margin</item>
        <item name="android:layout_marginRight">@dimen/gallery_toolbar_icon_margin</item>
    </style>

    <style name="GallerySpinner" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
    </style>

</resources>
